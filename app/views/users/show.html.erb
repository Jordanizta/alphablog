<h1 class="center">Bem vindo a página de <%= @user.username %></h1>
<div class="row">
	<div class="col-md-4 col-md-offset-4 center">
		<%= gravatar_for @user, size: 100 %>
	</div>
</div>
<h4 class="center">Artigos de <%= @user.username %></h4>
<div class="center">
	<%= will_paginate @user_articles %>
</div>
<%= render 'articles/index', articles: @user_articles %>
<div class="center">
	<%= will_paginate @user_articles %>
</div>
<%= link_to 'Voltar a listagem de blogueiros', users_path %>
